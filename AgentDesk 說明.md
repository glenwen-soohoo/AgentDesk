# AgentDesk 說明

> 建立日期：2026-09-21
> 類型：概覽
> 適用對象：拿到 AgentDesk 的人

---

## 這是什麼

AgentDesk 是一個桌面常駐小工具，把你本機正在跑的 AI session（Claude Code、Codex）畫成一間間等視角小辦公室裡工作的角色，讓你一眼看見：**誰在工作、誰在等你、等多久、誰卡住了**，不用一個個切視窗去確認。

## 怎麼跑

雙擊 `AgentDesk.exe`。視窗會出現在螢幕左上角（透明、常駐置頂）。

- 第一次執行若 Windows SmartScreen 跳警告 → 點「更多資訊」→「仍要執行」（未簽章的自製 app 都會這樣）。
- 不需要安裝 Node、不需要額外設定；app 會自己在背景啟動一個唯讀的 session 監控。

## 架構邏輯（資料怎麼流）

```text
本機 session 紀錄                偵測與正規化              分房與渲染
Claude ~/.claude/projects  ┐
                           ├─ monitor ─ adapter ─ router ─ 辦公室 tile + 角色 + 泡泡
Codex  ~/.codex/sessions   ┘
```

1. **monitor**：唯讀輪詢本機 session 紀錄，判斷每個 session 目前的狀態（工作中／等你／卡住／完成／閒置）與名稱。只讀、不修改、不外傳。
2. **adapter**：把不同來源的原始狀態，正規化成 AgentDesk 統一的狀態與優先級。
3. **router**：決定每個角色進哪一間辦公室（見下方規則）。
4. **渲染**：把角色放到辦公室的固定座位，配上狀態姿態與回報泡泡。

## 角色出現規則（精簡）

| 關卡 | 規則 |
|---|---|
| **會不會出現** | 只顯示目前有活動訊號的 session；沒在用、停置超過 30 分鐘的自動收起 |
| **進哪間辦公室** | 有回報對象（steward）→ 進該總管的辦公室；否則照名稱前綴歸群；再否則進 Direct 辦公室，滿了自動長下一間 |
| **坐哪** | 總管坐主桌後方，其餘 worker 各有固定座位 |
| **叫什麼** | 優先用 session 名稱；沒命名就用第一句你打的話當名字 |
| **什麼狀態** | 工作中 / 問你 / 等你 / 卡住 / 完成 / 待命。特別分開兩種「在等你」：Claude **跳選項問你**顯示「問你」（藍色），**回合結束、待你下一個指示**顯示「等你」（琥珀色）|
| **泡泡** | 每個角色都有自己的回報泡泡；點泡泡可開選單、確認或隱藏（關閉）該角色 |
| **關閉／復活角色** | 點泡泡選單「關閉角色」可把它從畫面隱藏（不影響來源 session）。一旦有隱藏的角色，畫面**右上角會出現一個小眼睛圖示鈕（角落帶數量）**，點它即可把關閉的角色全部復活 |
| **何時消失** | 關掉那個 session（結束 process）後，約 30 秒自動從畫面移除 |

## 隱私

AgentDesk **只在本機唯讀**你的 session 活動訊號（狀態、時間、名稱），**不會把對話內容送到任何外部服務**，也不會啟動、修改或終止你的 session。
